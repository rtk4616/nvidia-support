# Debconf is not used for "configuring" but only for resolving error
# conditions (i.e. leftover stray libraries) interactively.
nvidia-common: no-debconf-config
nvidia-common: postinst-uses-db-input

# The check for stray OpenGL libraries has been moved to a script that is
# called from several postinst scripts only.
nvidia-common: unused-debconf-template nvidia-common/remove-conflicting-libraries
nvidia-common: debconf-is-not-a-registry usr/lib/nvidia/check-for-conflicting-opengl-libraries
